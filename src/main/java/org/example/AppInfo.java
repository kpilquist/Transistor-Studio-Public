package org.example;

/**
 * Central place for application metadata shown in UI (About dialog, logs, etc.).
 */
public final class AppInfo {
    private AppInfo() {}

    /**
     * Human-friendly application version string.
     * TODO: Consider populating from Maven/POM or build metadata.
     */
    public static final String VERSION = "v0.9.0-BETA";
}
